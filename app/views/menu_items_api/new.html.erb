<div class='container'>
	<div class='a-tab'>
	<%= @the_menu.name %>
	<%= form_for [@the_menu, @the_item] do |f| %>
		<%= f.label :name %>
		<%= f.text_field :name %><br>
		<%= f.label :price %>
		$<%= f.text_field :price %><br>
		<%= f.label :description %>
		<%= f.text_area :description %> <br>

		<%= f.button %>

	<% end %>
</div>